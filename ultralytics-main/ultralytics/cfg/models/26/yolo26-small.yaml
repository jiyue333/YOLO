# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# YOLO26 small-object detection variant with P3/8 - P5/32 outputs.
# Uses standard one2many + NMS training/inference; no end2end one2one branch.

# Parameters
nc: 80 # number of classes
end2end: False # standard NMS inference/validation path
prog_loss: False # disable one2one/progressive branch to reduce training cost
reg_max: 1 # DFL bins
scales:
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# YOLO26n backbone, unchanged
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5, 3, True]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# Small-object neck/head variant
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2, [512, True]] # 13

  - [[-1, 4], 1, FreqFusion, [256]] # frequency-aware P4->P3 fusion
  - [-1, 2, C3k2, [256, True]] # 15
  - [[-1, 2], 1, SAF, [256]] # inject shallow P2/4 detail into P3
  - [-1, 1, CoordAtt, []] # 17 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2, [512, True]] # 20
  - [[-1, 4], 1, SAF, [512]] # inject backbone P3/8 detail into P4
  - [-1, 1, CoordAtt, []] # 22 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 1, C3k2, [1024, True, 0.5, True]] # 25 (P5/32-large)

  - [[17, 22, 25], 1, Detect, [nc]] # Detect(P3, P4, P5)
